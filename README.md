# Tiny-Autograd-Engine-Dementia-Classifier
A scalar-valued automatic differentiation engine built from scratch in pure Python, with a small neural-network library on top, trained to classify dementia from OASIS-style clinical features. No PyTorch or TensorFlow is used for the learning itself; every gradient is computed by the engine in engine.py.
